# Cloudflare 워커(정적 자산) 배포 설정.
# 게임 허브는 서버 기능이 없는 순수 정적 사이트 → next build(output:export)가 만든
# out/ 폴더를 정적 자산으로 그대로 서빙한다. `npx wrangler deploy` 로 배포.
name = "ezssam-games"
compatibility_date = "2025-05-01"

[assets]
directory = "./out"
not_found_handling = "404-page"
